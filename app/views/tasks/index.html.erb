<h1>タスク一覧</h1>
<ul>
  <% @tasks.each do |task| %>
  <li><%= task.content %>：<%= link_to "詳細ページへ",task %></li>
  <div>ステータス：<%= task.status %></div>
  <% end %>
</ul>

<%= link_to '新規作成', new_task_path %>